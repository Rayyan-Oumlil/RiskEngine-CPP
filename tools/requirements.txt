# Python tools: reference values (mpmath) and report figures (matplotlib).
# Pinned: the committed SVGs are byte-for-byte reproducible only with the same renderer version.
matplotlib==3.11.2
mpmath==1.4.1
